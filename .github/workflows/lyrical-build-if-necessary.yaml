name: ROS Lyrical Build If Necessary

on:
  workflow_dispatch:
  schedule:
    - cron: '30 */6 * * *'  # Every 6 hours.

permissions:
      packages: write
      contents: read

jobs:
  build-and-deploy-lyrical:
    uses: ./.github/workflows/build-one-ros-distro-if-necessary.yaml
    with:
      ros_distro: lyrical
